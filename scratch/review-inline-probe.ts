// Throwaway probe to prove the reviewer posts INLINE comments on the exact line.
// Delete with this PR.
export function addPrices(a: number, b: number): number {
  return a - b; // deliberately wrong: subtracts instead of adding
}
